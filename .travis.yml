dist: trusty
sudo: false
services:
  - docker
env:
  TRAVIS_BRANCH_CLEAN=$(sed 's/\//-/g' <<< $TRAVIS_BRANCH)
  DOCKER_TAG="$TRAVIS_COMMIT.$TRAVIS_BRANCH_CLEAN"
  APP_NAME=skeleton-haskell
script: |
  set -e

  docker build --tag imageintelligence/$APP_NAME:$DOCKER_TAG .
